// Optional local PostHog configuration. Copy this file to Secrets.xcconfig
// (which is ignored by git) and replace the placeholder with a PostHog phc_
// project token. Do not use a personal or administrative API key.
POSTHOG_PROJECT_API_KEY = YOUR_POSTHOG_PROJECT_TOKEN
