// Optional local signing configuration. Copy this file to Signing.xcconfig
// (which is ignored by git) and replace the placeholder with your Apple Team ID.
PINATIVE_DEVELOPMENT_TEAM = YOUR_APPLE_TEAM_ID
